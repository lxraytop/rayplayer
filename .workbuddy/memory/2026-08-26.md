# 2026-08-26 工作日志

## Ray Player 1.5.0 品牌重构（Folia 0.7.0 → Ray 1.5.0）

- **工作目录**：`F:\ProjectAI\rayplayer\1.5.0`（原为 Folia 0.7.0 源码，已就地重构为 Ray 1.5.0）
- **依据**：`重构详细报告.md`（v1.1.0）+ `重构详细报告v1.2.0.md` 合并执行
- **批量替换**：Python 脚本（`F:\ProjectAI\rayplayer\refactor_scripts\brand_replace.py`）243 文件 / 1180 处替换，含 0.7.0 特有 token（foliaignore→rayignore、folia-cache、folia-egress/internal/edge 等 Docker 网络）
- **重命名**：folia-grid→ray-grid、useFoliaHexViewport→useRayHexViewport、foliaIgnore.ts→rayIgnore.ts、packaging/aur/folia-major-bin→ray-player-bin、folia-major.desktop/png→ray-player、public/folia-cover-sw.js→ray-cover-sw.js、prepare-folia-release skill→prepare-ray-release、3 个预发布 workflow 禁用（.yml.disabled）
- **核心配置**：package.json（ray-player/Ray/1.5.0/角完/top.lxray.player/lxraytop/rayplayer）、wrangler/metadata/realeco-release=1.5.0、.all-contributorsrc 与 CONTRIBUTORS.md 清空贡献者
- **更新系统**：updateChannels.cjs 重写为仅 stable 通道；main.cjs 新增 compareVersions() 并接入 update-available 防御比较；SettingsModal/DesktopSettingsSubview 移除通道下拉与夸克/百度网盘下载；i18n 移除国内下载词条、新增 updateChannelStable
- **CI/CD**：electron-release.yml 重写为 tag 触发（v*）、stable 通道、保留 windowtolayer 构建与 AUR 发布
- **其他**：vite.config.ts 移除 namoe API、默认通道 stable；歌词预览标题→Legend Never Die / 何慕卿 (徐良)；USER_GUIDE_AUTO_OPEN_VERSION→1.5.0；i18n releaseNotes key v0_7_0→v1_5_0
- **图像**：build/icon.png、头像 2-12、表情包从 1.1.0/ray-player 参考版复制；public/icon.svg 重制为紫色 R 字母；miao.png/托盘/缩略图/占位封面按报告保持待替换
- **保留**：folia-site.cielaniska.top 域名（报告明确保留）、public/icon.svg.bak 备份、shared/realecoReleaseMetadata.cjs（死代码，测试仍通过）
- **注意事项**：package-lock.json 曾被系统文件锁占用（EPERM），沙箱外删除后 npm 重新生成；i18n 等文件被锁时只能用 Edit 工具写入

## 补充：预览占位符换真实歌曲 + 类型修复

- `src/components/visualizer/PreviewPlaceholder.ts` 歌词数据替换为真实歌曲：default → Legend Never Die（Against The Current，LoL 2017 全球总决赛主题曲，英文歌词+中文翻译）；reserved → 何慕卿（徐良，中文歌词+英文翻译+拼音罗马音）。标题保持 Legend Never Die / 何慕卿 (徐良)
- i18n 三语言 `previewTextDefault`/`previewTextReserved` 词条同步更新（zh: Legend Never Die / 何慕卿 (徐良)；en/in: Legend Never Die / He Mu Qing (Xu Liang)）
- 修复 SettingsModal.tsx `DEFAULT_UPDATE_CHANNEL = 'stable' as const` 类型推断问题（UPDATE_CHANNEL: string 与 ElectronSettingsState 的 'stable' 字面量不兼容），tsc --noEmit 零错误
- 用户自行运行：node24 (F:\ProjectAI\rayplayer\node24) + npm install（kugoumusicapi 已预复制）；Web 端 `npm run dev` / `npm run build`；electron 二进制未就绪（打包前需 `node node_modules/electron/install.js`）

## 补充：发布流水线脚本

- 新增 `release-ray.sh`（Git Bash）与 `release-ray.bat`（Windows CMD）一键发布脚本：git init（如需要）→ commit → tag v1.5.0 → push tag → force push main → 补装 electron → `npm run build:electron` 打包 exe → 验证（ls-remote 确认 tag/分支、检查 Ray-Setup-1.5.0.exe）
- 1.5.0 目录尚无 .git，脚本内置 git init + remote add（https://github.com/lxraytop/rayplayer.git）
- CI/CD 无需额外配置：electron-release.yml 已重写为 `push tags: v*` 触发，三平台构建（Windows 生成 exe）、创建 Release、发布 AUR
- 发布顺序要点：先推 tag（触发 CI 构建）再 force main；force 推送会覆盖远程 main，需确认无他人提交
